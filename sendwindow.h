#ifndef SENDWINDOW_H
#define SENDWINDOW_H

#include <QMainWindow>

class SendWindow : public QMainWindow
{
    Q_OBJECT
public:
    SendWindow(QWidget *parent = 0);
signals:

public slots:

};

#endif // SENDWINDOW_H
